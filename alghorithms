#include<bits/stdc++.h>
using namespace std;
int main()
{
    int ans = 0,sum = 0;
    int n;cin>>n;vector<int> a(n);
    for(int i=0;i<n;i++)cin>>a[i];
    ans = sum = a[0];
    for(int i=0;i<n;i++)
    {
        sum=max(sum+a[i],a[i]);
        ans=max(ans,sum);
    }
    cout<<ans<<endl;
}
